{
  "name": "@amex/vitruvius",
  "version": "1.0.1",
  "description": "Add buildInitialState method to your reducers",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "prebuild": "npm run lint && npm run clean",
    "build": "babel src -d lib --copy-files",
    "lint": "eslint --ignore-path .eslintignore ./src/**/*.js ./test/**/*.js",
    "pretest": "npm run lint",
    "test": "BABEL_ENV=test istanbul cover _mocha -- 'test/**/*.spec.js' --compilers js:babel-register",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "ssh:****.com/uie/vitruvius.git"
  },
  "author": "Jimmy King <jimmy.king@aexp.com>",
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-core": "^6.17.0",
    "babel-eslint": "^6.1.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.16.3",
    "chai": "^3.5.0",
    "eslint": "^3.6.0",
    "eslint-config-axp": "^1.0.0",
    "immutable": "^3.8.1",
    "istanbul": "^1.0.0-alpha.2",
    "mocha": "^3.0.2",
    "rimraf": "^2.5.4",
    "sinon": "^1.17.6"
  },
  "dependencies": {
    "redux": "^3.6.0"
  },
  "publishConfig": {
    "registry": "****.com/artifactory/api/npm/npm-virtual"
  }
}
